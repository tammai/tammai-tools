StylesPath = styles
MinAlertLevel = warning

[*.md]
BasedOnStyles = TamMai, TamMaiVI

# Emoji are load-bearing in social copy — they mark structure and tone.
# Every other rule stays on: AI slop words are worse on LinkedIn than
# in a README, not better.
TamMai.Emoji = NO
